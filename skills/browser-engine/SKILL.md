---
name: browser-engine
description: Operate and extend isolated browser sessions with explicit policy, state, and resource boundaries.
---

# Browser Engine

Use the existing Playwright adapter and policy gates. Validate URL schemes, redirects, private-network access, session ownership, uploads, downloads, and persistent state. Prefer accessibility targets over arbitrary script execution. Keep browser startup explicit and test snapshots and advanced actions against a local page.
