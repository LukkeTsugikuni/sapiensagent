---
name: integration
description: Integrate providers, channels, scheduler, memory, MCP, plugins, browser, and computer use through shared policy.
---

# Integration

Treat every adapter as capability-driven. Preserve the common request/session/receipt model, route only to providers that support the requested modality, use bounded retries and fallback, and keep external writes behind approval. A capability is ready only after a local contract test and a redacted failure path exist.
